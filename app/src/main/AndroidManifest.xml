<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.app.observablestudy">

    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ObservableStudy">
        <activity
            android:name=".Ui.RecyclerActivity"
            android:exported="false" />

        <service android:name=".Services.BackgroundServices" />
        <service android:name=".Services.ForegroundService" />
        <service android:name=".Services.MediaPlayerService" />

        <activity
            android:name=".Ui.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".Ui.ServiceActivity"
            android:exported="false" />
        <activity
            android:name=".Ui.StudyBasicsActivity"
            android:exported="false" />

        <receiver
            android:name=".BroadcastReceiver.RebootBroadcastReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>
    </application>

</manifest>